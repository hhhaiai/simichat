# ChatGPT UI 界面分析

## 整体布局

采用经典的三栏式布局：侧边栏 + 主内容区 + 输入区。

```
┌──────────┬────────────────────────────────────┐
│          │            顶部导航栏               │
│  侧边栏  ├────────────────────────────────────┤
│          │                                    │
│  对话列表 │         对话主区域                  │
│          │      (用户消息 / AI 回复)            │
│          │                                    │
│          ├────────────────────────────────────┤
│          │         底部输入区                  │
└──────────┴────────────────────────────────────┘
```

---

## 1. 左侧边栏（Sidebar）

### 顶部操作
- **New Chat** 按钮 — 新建对话
- 搜索栏 — 搜索历史对话

### 对话历史列表
- 按时间分组：Today、Yesterday、Previous 7 Days、Previous 30 Days
- 每条对话可重命名、归档、删除
- 鼠标悬停显示操作菜单（...）

### 底部区域
- 用户头像 + 名称
- 设置入口（Settings）
- 升级入口（Upgrade to Plus/Pro）

### 侧边栏状态
- 可折叠/展开
- 折叠后仅显示图标

---

## 2. 顶部导航栏（Header）

- 当前对话名称 / 模型名称
- 模型选择器下拉菜单（GPT-4o、o1、o3、GPT-4o mini 等）
- 分享按钮
- 更多操作菜单

---

## 3. 对话主区域（Chat Area）

### 用户消息
- 右对齐或左对齐（不同版本有差异）
- 显示用户头像
- 支持编辑已发送的消息（铅笔图标）
- 支持复制消息内容

### AI 回复
- 左对齐，带 AI 头像/图标
- Markdown 渲染：标题、粗体、斜体、列表、表格
- 代码块：语法高亮 + 一键复制 + 运行按钮
- 数学公式：LaTeX 渲染（KaTeX）
- 图片内联显示

### 消息操作栏
- 复制（Copy）
- 重新生成（Regenerate）
- 点赞/点踩（Good / Bad response）
- 编辑（Edit）

### 流式输出
- 逐字/逐 token 打字效果
- 可随时停止生成（Stop generating）

---

## 4. 底部输入区（Input Area）

### 输入框
- 多行文本输入，自动增高
- Placeholder 提示文字
- Shift+Enter 换行，Enter 发送

### 附件功能
- 图片上传（本地 / 拍照）
- 文件上传（PDF、文档等）
- 拖拽上传支持

### 工具按钮
- **附件/上传** 按钮（+ 号）
- **联网搜索** 开关（Search the web）
- **图片生成** 开关（DALL-E）
- **Canvas** 开关（文档/代码编辑器）
- **发送按钮**（箭头图标）

### 免责声明
- "ChatGPT can make mistakes" 提示文字

---

## 5. 特色功能界面

### Canvas（画布编辑器）
- 右侧弹出的文档/代码编辑面板
- 支持实时编辑、版本对比
- 代码可直接运行

### GPTs 商店
- 浏览/搜索自定义 GPT
- 分类展示：DALL-E、Writing、Productivity、Programming 等

### 联网搜索结果
- 搜索来源引用卡片
- 可展开查看引用详情

### 图片生成
- DALL-E 生成的图片内联显示
- 支持继续编辑、变体生成

---

## 6. 响应式设计

| 屏幕尺寸 | 行为 |
|----------|------|
| 桌面端 | 完整三栏布局 |
| 平板端 | 侧边栏可收起 |
| 移动端 | 侧边栏默认隐藏，通过汉堡菜单展开 |

---

## 7. 交互细节

- **Enter** 发送消息 / **Shift+Enter** 换行
- 对话自动保存到历史
- 支持对话分享（生成公开链接）
- 深色/浅色主题切换
- 多语言支持
- 键盘快捷键支持
